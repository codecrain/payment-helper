'use strict';

const router   = require('express')();
module.exports = router;

/**
 *  [논의사항]
 */

router.get('/product/hashtags', function(req, res, next) {
    // 해당 해시태그에 속하는 상품목록 불러오기
});

// 이 부분은 추가적인 논의 필요함.